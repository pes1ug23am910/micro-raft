[package]
name = "sim"
description = "Deterministic fault-injection simulator for raft-core"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

# The simulator executes the same core against virtualized effects.
[dependencies]
raft-core = { path = "../raft-core" }
