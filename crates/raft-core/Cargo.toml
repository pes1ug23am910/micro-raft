[package]
name = "raft-core"
description = "Pure, deterministic Raft state machine (sans-I/O): inputs in, effects out"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

# Keep the consensus core I/O-free and its dependency surface minimal.
[dependencies]
serde = { workspace = true }
